# 上下文管理

> 系统提示、用户消息、助手回复、工具调用与结果。

1. 有效窗口：模型窗口 - 预留输出。预留空间给压缩操作本身留位置。
    * 窗口会随时计算，提示。
    * 自动压缩，连续失败达到阈值后熔断，避免雪崩效应
    * 4级压缩：snip剪裁-清除旧的工具结果内容，替换为标记文本；microcompact微压缩-时间机制，认为过期了；collapse折叠-重构消息组；autocompact-自动压缩，调用 LLM 对完整对话进行摘要。
2. 压缩的质量取决于prompt的设计：明确说明记什么、不记什么、用什么格式。